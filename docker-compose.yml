version: '3.8'

services:
  emoji-hunt:
    build: .
    ports:
      - "3000:80"
    container_name: emoji-hunt-game
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    
  # Optional: Add a reverse proxy for production
  # nginx-proxy:
  #   image: nginx:alpine
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./proxy.conf:/etc/nginx/nginx.conf
  #   depends_on:
  #     - emoji-hunt